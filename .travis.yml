language: shell

os:
  - osx
  - linux

script:
  - tar -xvzf OpenMEEG*.tar.gz
  - cd OpenMEEG*
  - pwd
  - ls -lR
  - ldd ./bin/om_xxx
  - ldd ./lib/libOpenMEEGMaths.so
  - ./bin/om_xxx
 
branches:
  only:
    - master

# trigger travis
